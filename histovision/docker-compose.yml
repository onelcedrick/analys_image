services:
  histovision:
    build: .
    ports:
      - "7860:7860"
    environment:
      - HISTOVISION_STORAGE_BACKEND=local
      - HISTOVISION_DATABASE_BACKEND=sqlite
      - PORT=7860
    volumes:
      - hv_storage:/app/storage
    restart: unless-stopped

volumes:
  hv_storage:
